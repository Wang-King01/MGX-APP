# Project Summary

“周末出逃”是面向大学生的本地周末活动探索指南 Demo，帮助用户根据兴趣、天气、预算和同行人数发现展览、市集、演出及短途徒步等活动，并完成从查找活动到组队、打卡和攻略分享的体验闭环。当前使用演示活动、天气和队伍数据，交互结果仅保存在浏览器中，不涉及真实报名、支付或公开运营。

# Project Module Description

- **活动推荐与筛选**：按兴趣、天气、预算和同行人数筛选演示活动，并查看活动详情。
- **活动收藏**：收藏或取消收藏感兴趣的活动。
- **组队演示**：查看队伍信息并体验加入组队流程，明确标注为 Demo 交互。
- **打卡记录**：记录活动打卡手记，数据保存在当前浏览器。
- **攻略分享**：复制或分享活动攻略内容。
- **响应式界面**：适配桌面端和移动端，全部页面与文案使用中文。
- **主题视觉**：采用 Golden Honey 风格，以蜂蜜金、暖奶油底色和深棕文字构建统一视觉。
- **后端基础能力**：提供健康检查、认证、用户、设置、存储及 AI Hub 等通用 API 模块，为后续产品化扩展预留基础。

# Directory Tree

```text
app/
├── .mgx/                         # 项目工具配置
├── start_app_v2.sh               # 应用启动脚本
├── backend/
│   ├── main.py                   # FastAPI 应用入口
│   ├── lambda_handler.py         # Serverless 请求处理入口
│   ├── requirements*.txt         # Python 依赖
│   ├── alembic.ini               # 数据库迁移配置
│   ├── alembic/                  # Alembic 环境与迁移模板
│   ├── core/                     # 配置、环境、认证、数据库、加密与遥测
│   ├── dependencies/             # 认证和数据库依赖注入
│   ├── models/                   # 数据模型与基础模型
│   ├── schemas/                  # API 请求和响应结构
│   ├── routers/                  # 健康、认证、用户、设置、存储和 AI Hub 路由
│   ├── services/                 # 用户、认证、数据库、存储、支付和 AI Hub 服务
│   ├── middlewares/              # Web 中间件
│   ├── utils/                    # 日志等通用工具
│   └── skills_docs/              # AI 能力、自定义 API、对象存储和 Web SDK 文档
└── frontend/
    ├── package.json              # 前端依赖和脚本
    ├── vite.config.ts            # Vite 构建配置
    ├── tailwind.config.ts        # Tailwind 配置
    ├── src/
    │   ├── App.tsx               # 应用路由与页面壳
    │   ├── App.css/index.css     # 全局及应用样式
    │   ├── main.tsx              # 前端入口
    │   ├── pages/                # 首页、认证回调和博客页面
    │   ├── components/           # 加载、权限、博客和 UI 组件
    │   ├── contexts/             # 登录状态上下文
    │   ├── api/                  # 设置等 API 调用
    │   ├── lib/                  # API、认证、博客、配置和工具函数
    │   ├── hooks/                # 移动端与通知 Hooks
    │   └── explore-data.ts       # 城市探索演示活动数据
    ├── public/                   # favicon 与 robots 文件
    ├── prerender/                # 博客预渲染与站点地图脚本
    └── *.config.*                # TypeScript、ESLint、PostCSS 等工程配置
assets/images/                    # 城市探索场景与活动配图
uploads/                          # 项目生成或上传的图片资源
```

# File Description Inventory

| 文件/目录 | 说明 |
|---|---|
| `backend/main.py` | 创建并配置后端 Web 应用及路由 |
| `backend/core/` | 管理环境变量、数据库连接、认证、加密和请求/SQL 遥测 |
| `backend/routers/` | 暴露健康检查、认证、用户、设置、存储和 AI Hub 接口 |
| `backend/services/` | 封装业务服务、演示数据、对象存储和 AI Hub 调用 |
| `backend/models/`、`schemas/` | 定义持久化模型及接口数据结构 |
| `backend/alembic/` | 管理数据库迁移 |
| `backend/skills_docs/` | 记录后端扩展能力与接入规范 |
| `frontend/src/pages/Index.tsx` | 城市探索 Demo 主页面 |
| `frontend/src/explore-data.ts` | 活动、天气、队伍等本地演示数据 |
| `frontend/src/components/` | 页面业务组件、博客组件及通用 UI 组件 |
| `frontend/src/contexts/AuthContext.tsx` | 管理前端认证状态 |
| `frontend/src/lib/api.ts`、`api/settings.ts` | 前后端接口及设置调用封装 |
| `frontend/src/lib/auth.ts` | 前端认证辅助逻辑 |
| `frontend/src/App.css`、`src/index.css` | Golden Honey 主题变量、布局、响应式与交互样式 |
| `frontend/package.json` | 前端依赖、开发和生产构建命令 |
| `frontend/vite.config.ts`、`tailwind.config.ts` | 构建工具与样式系统配置 |
| `assets/images/` | 首页 Hero 和活动卡片使用的城市图片 |
| `uploads/` | 生成的图片资源 |

# Technology Stack

- **前端**：React、TypeScript、Vite
- **样式与组件**：Tailwind CSS、CSS Variables、shadcn/ui 风格组件、Radix UI 生态组件
- **后端**：Python、FastAPI、Pydantic
- **数据层**：SQLAlchemy、Alembic
- **认证与安全**：后端认证依赖、加密工具、环境配置
- **工程质量**：ESLint、TypeScript、PostCSS
- **部署适配**：支持常规 Web 服务及 Serverless Lambda Handler
- **数据形态**：当前 Demo 主要使用前端本地演示数据和浏览器存储

# Usage

## 前端安装与运行

```bash
cd app/frontend
npm install
npm run dev
```

## 前端生产构建

```bash
cd app/frontend
npm run build
```

## 前端代码检查

```bash
cd app/frontend
npm run lint
```

## 后端安装与运行

```bash
cd app/backend
python -m venv .venv
source .venv/bin/activate
pip install -r requirements.txt
uvicorn main:app --reload
```

Windows 激活虚拟环境可使用：

```bash
.venv\Scripts\activate
```

## 数据库迁移

```bash
cd app/backend
alembic upgrade head
```

也可根据项目环境使用根目录启动脚本：

```bash
cd app
bash start_app_v2.sh
```
